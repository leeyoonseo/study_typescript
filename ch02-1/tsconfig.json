{
  // @see https://typescript-kr.github.io/pages/compiler-options.html
  "compilerOptions": {
    // 호환되는 JavaScript 언어 버전 설정
    "target": "es5",         
    
    // 모듈 코드 지정
    "module": "commonjs",         
    
    // 모듈 해석, 컴파일러 import가 무엇을 참조하는지 알아내기 위해 사용하는 프로세스
    "moduleResolution": "node",        
    
    // 기본 디렉토리 지정
    "baseUrl": ".",
    
    // baseUrl을 기준으로 관련된 위치에 모듈 이름의 경로 매핑 목록을 나열
    "paths": { "*": ["node_modules/*"] }, 
    
    // 내보낸 JavaScript 파일에 대한 소스 맵 파일 생성
    "sourceMap": true,

    // 내보낸 모든 파일의 출력 폴더 지정
    "outDir": "dist",      

    // ES5 또는 ES3를 대상으로 할 때 for..of, 스프레드와 구조분해할당에서 이터러블 지원
    "downlevelIteration": true,   

    // 런타임 바벨 생태계 호환성을 위한 __importStar와 __importDefault 헬퍼를 내보내고 타입 시스템 호환성을 위해 활성화
    "esModuleInterop": true,        

    // any 타입으로 암시한 표현식과 선언에 오류를 발생에 대한 활성화 여부
    "noImplicitAny": false,   
  },
  "include": ["src/**/*"]
}
